#conda activate envTosave
#conda config --set channel_priority flexible
conda create -n envTosave
conda activate envTosave
cd toDirRequirements # (where "myenvironment.yml" is saved)
conda export > myenvironment.yml

Note: now change the name of the environment to be created which is in the first line of myenvironment.yml

Then do:

conda env create --file myenvironment.yml